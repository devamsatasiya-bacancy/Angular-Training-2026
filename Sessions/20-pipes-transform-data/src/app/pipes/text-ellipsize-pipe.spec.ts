import { TextEllipsizePipe } from './text-ellipsize-pipe';

describe('TextEllipsizePipe', () => {
  it('create an instance', () => {
    const pipe = new TextEllipsizePipe();
    expect(pipe).toBeTruthy();
  });
});
