import { HighlightDiscount } from './highlight-discount';

describe('HighlightDiscount', () => {
  it('should create an instance', () => {
    const directive = new HighlightDiscount();
    expect(directive).toBeTruthy();
  });
});
