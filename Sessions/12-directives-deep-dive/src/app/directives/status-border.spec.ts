import { StatusBorder } from './status-border';

describe('StatusBorder', () => {
  it('should create an instance', () => {
    const directive = new StatusBorder();
    expect(directive).toBeTruthy();
  });
});
