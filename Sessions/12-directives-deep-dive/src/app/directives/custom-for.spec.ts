import { CustomFor } from './custom-for';

describe('CustomFor', () => {
  it('should create an instance', () => {
    const directive = new CustomFor();
    expect(directive).toBeTruthy();
  });
});
