import { HoverScale } from './hover-scale';

describe('HoverScale', () => {
  it('should create an instance', () => {
    const directive = new HoverScale();
    expect(directive).toBeTruthy();
  });
});
